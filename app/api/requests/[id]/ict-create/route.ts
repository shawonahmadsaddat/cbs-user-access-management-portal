import {NextResponse} from "next/server";import {db} from "@/lib/db";import {mockFinacleCreate} from "@/lib/integrations";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const r=await db.accessRequest.findUnique({where:{id}});if(r){const x=await mockFinacleCreate(r.employeeId);await db.accessRequest.update({where:{id},data:{status:"ICT_CREATED",finacleUserId:x.userId}});await db.auditLog.create({data:{requestId:id,actorId:"ICTM001",action:"ICT_CREATE",details:x.userId}})}return NextResponse.redirect("http://localhost:3000/ict")}