import {NextResponse} from "next/server";import {db} from "@/lib/db";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;await db.accessRequest.update({where:{id},data:{status:"RETURNED"}});await db.auditLog.create({data:{requestId:id,actorId:"DEMO_APPROVER",action:"RETURN",details:"Returned for correction"}});return NextResponse.redirect("http://localhost:3000/approvals")}