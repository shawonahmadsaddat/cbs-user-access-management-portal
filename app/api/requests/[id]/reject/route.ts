import {NextResponse} from "next/server";import {db} from "@/lib/db";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;await db.accessRequest.update({where:{id},data:{status:"REJECTED"}});await db.auditLog.create({data:{requestId:id,actorId:"DEMO_APPROVER",action:"REJECT",details:"Rejected"}});return NextResponse.redirect("http://localhost:3000/approvals")}